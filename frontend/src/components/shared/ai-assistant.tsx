// [DEPRECATED] Chatbot feature removed per user request. Kept for reference only.
// Uncomment and restore imports in client-providers.tsx to re-enable.

"use client";  import { useState, useEffect, useRef, useCallback } from "react"; import { aiChat } from "@/lib/api/ai";  type Message = {   role: "user" | "assistant";   text: string; };  const GREETING = "Hi! I'm the Accredit.vip assistant. Ask me about creating events, managing guests, sending invites, ticketing, QR codes, or anything else!";  export function AIAssistant({ open: initialOpen = false }: { open?: boolean } = {}) {   const [open, setOpen] = useState(initialOpen);   const [messages, setMessages] = useState<Message[]>([{ role: "assistant", text: GREETING }]);   const [input, setInput] = useState("");   const [loading, setLoading] = useState(false);   const [pos, setPos] = useState({ x: 0, y: 0 });   const [dragging, setDragging] = useState(false);   const [showTooltip, setShowTooltip] = useState(true);   const dragStart = useRef({ x: 0, y: 0, posX: 0, posY: 0 });   const messagesEndRef = useRef<HTMLDivElement>(null);   const tooltipTimer = useRef<ReturnType<typeof setTimeout> | null>(null);    useEffect(() => {     if (typeof window !== "undefined") {       const right = window.innerWidth <= 640 ? 12 : 24;       const bottom = 80;       setPos({ x: right, y: bottom });     }   }, []);    useEffect(() => {     messagesEndRef.current?.scrollIntoView({ behavior: "smooth" });   }, [messages]);    useEffect(() => {     if (open) {       setShowTooltip(false);       if (tooltipTimer.current) clearTimeout(tooltipTimer.current);       return;     }     const timer = setTimeout(() => setShowTooltip(true), 5000);     tooltipTimer.current = timer;     return () => { clearTimeout(timer); };   }, [open]);    const startDrag = useCallback((clientX: number, clientY: number) => {     setDragging(true);     dragStart.current = {       x: clientX,       y: clientY,       posX: pos.x,       posY: pos.y,     };     setShowTooltip(false);   }, [pos]);    const handleMouseDown = useCallback((e: React.MouseEvent) => {     startDrag(e.clientX, e.clientY);   }, [startDrag]);    const handleTouchStart = useCallback((e: React.TouchEvent) => {     if (e.touches.length > 0) {       startDrag(e.touches[0].clientX, e.touches[0].clientY);     }   }, [startDrag]);    useEffect(() => {     if (!dragging) return;     const clamp = (n: number, max: number) => Math.max(0, Math.min(n, max));     const handleMouseMove = (e: MouseEvent) => {       const dx = e.clientX - dragStart.current.x;       const dy = e.clientY - dragStart.current.y;       const maxX = typeof window !== "undefined" ? window.innerWidth - 80 : 200;       const maxY = typeof window !== "undefined" ? window.innerHeight - 120 : 200;       setPos({         x: clamp(dragStart.current.posX - dx, maxX),         y: clamp(dragStart.current.posY - dy, maxY),       });     };     const handleTouchMove = (e: TouchEvent) => {       if (e.touches.length > 0) {         const dx = e.touches[0].clientX - dragStart.current.x;         const dy = e.touches[0].clientY - dragStart.current.y;         const maxX = typeof window !== "undefined" ? window.innerWidth - 80 : 200;         const maxY = typeof window !== "undefined" ? window.innerHeight - 120 : 200;         setPos({           x: clamp(dragStart.current.posX - dx, maxX),           y: clamp(dragStart.current.posY - dy, maxY),         });       }     };     const handleEnd = () => setDragging(false);     window.addEventListener("mousemove", handleMouseMove);     window.addEventListener("mouseup", handleEnd);     window.addEventListener("touchmove", handleTouchMove);     window.addEventListener("touchend", handleEnd);     return () => {       window.removeEventListener("mousemove", handleMouseMove);       window.removeEventListener("mouseup", handleEnd);       window.removeEventListener("touchmove", handleTouchMove);       window.removeEventListener("touchend", handleEnd);     };   }, [dragging]);    const handleSend = async (e: React.FormEvent) => {     e.preventDefault();     const text = input.trim();     if (!text || loading) return;      const userMsg: Message = { role: "user", text };     setMessages((prev) => [...prev, userMsg]);     setInput("");     setLoading(true);      try {       const reply = await aiChat([...messages, userMsg]);       setMessages((prev) => [...prev, { role: "assistant", text: reply }]);     } catch {       setMessages((prev) => [...prev, { role: "assistant", text: "Sorry, I couldn't process that. Try again later." }]);     } finally {       setLoading(false);     }   };    return (     <>       {open && (         <div           className="fixed z-50 w-[calc(100vw-2rem)] sm:w-[380px] max-h-[520px] bg-background border rounded-2xl shadow-2xl flex flex-col overflow-hidden"           style={{ bottom: `${pos.y + 60}px`, right: `${pos.x}px` }}         >           <div             className="flex items-center justify-between px-4 py-3 border-b bg-primary text-primary-foreground cursor-grab active:cursor-grabbing"             onMouseDown={handleMouseDown}             onTouchStart={handleTouchStart}           >             <span className="text-sm font-semibold">Accredit.vip Assistant</span>             <button onClick={() => setOpen(false)} className="text-primary-foreground/80 hover:text-primary-foreground">               <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="M18 6 6 18" /><path d="m6 6 12 12" /></svg>             </button>           </div>           <div className="flex-1 overflow-y-auto p-4 space-y-3 text-sm">             {messages.map((m, i) => (               <div key={i} className={`flex ${m.role === "user" ? "justify-end" : "justify-start"}`}>                 <div className={`max-w-[80%] rounded-xl px-3 py-2 ${                   m.role === "user"                     ? "bg-primary text-primary-foreground"                     : "bg-muted text-foreground"                 }`}>                   {m.text}                 </div>               </div>             ))}             <div ref={messagesEndRef} />           </div>           <form onSubmit={handleSend} className="border-t p-3 flex gap-2">             <input               type="text"               placeholder="Ask something..."               value={input}               onChange={(e) => setInput(e.target.value)}               className="flex-1 h-9 rounded-lg border border-input bg-background px-3 py-2 text-sm"             />               <button type="submit" disabled={loading} className="h-9 px-3 rounded-lg bg-primary text-primary-foreground text-sm font-medium disabled:opacity-50">{loading ? "..." : "Send"}</button>           </form>         </div>       )}       <div         className="fixed z-50 touch-none"         style={{ bottom: `${pos.y}px`, right: `${pos.x}px` }}         onMouseDown={handleMouseDown}         onTouchStart={handleTouchStart}       >         <div className="relative">           {showTooltip && !open && (             <div className="absolute bottom-full right-0 mb-3 whitespace-nowrap bg-foreground text-background text-xs font-medium px-3 py-1.5 rounded-lg shadow-lg animate-pulse">               Need help? Ask me anything!               <div className="absolute top-full right-4 -mt-px w-2 h-2 bg-foreground rotate-45" />             </div>           )}           <button             onClick={() => { if (!dragging) setOpen(!open); }}             className="w-12 h-12 rounded-full bg-primary text-primary-foreground shadow-lg hover:opacity-90 transition-all flex items-center justify-center select-none"             title="Ask the AI assistant"           >             {open ? (               <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="M18 6 6 18" /><path d="m6 6 12 12" /></svg>             ) : (               <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" /></svg>             )}           </button>         </div>       </div>     </>   ); }
